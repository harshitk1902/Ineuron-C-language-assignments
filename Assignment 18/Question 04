void uppercase(char []);

// function to transform string into uppercase

void uppercase(char str[])
{
    int i ;
    for(i=0 ; str[i] ; i++)
    {
        if(str[i]>='a' && str[i]<='z')
        str[i] = str[i] - 32 ;
    }
    printf("\nconverted string is : %s" , str);
}