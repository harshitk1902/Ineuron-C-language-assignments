void lowercase(char []);

// function to transform string into lowercase

void lowercase(char str[])
{
    int i ;
    for(i=0 ; str[i] ; i++)
    {
        if(str[i]>='A' && str[i]<='Z')
        str[i] = str[i] + 32 ;
    }
    printf("\nconverted string is : %s" , str);
}